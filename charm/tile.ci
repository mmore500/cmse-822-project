module tile {

  array [1D] Tile {
    entry Tile();
    entry void seedGen(double lastSeedGenTime);
    entry [expedited] void takeTap(
        size_t waveCountdown,
        size_t outDirection,
        size_t channelID,
        size_t seedID
      );
    };

};
