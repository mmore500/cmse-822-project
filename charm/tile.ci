module tile {

  array [1D] Tile {
    entry Tile();
    entry void setChan(size_t channelID);
    entry void seedGen(double lastSeedGenTime);
    entry [expedited] void takeTap(
        double firstTapTime,
        size_t firstTapX,
        size_t firstTapY,
        size_t outDirection,
        size_t channelID,
        size_t seedID
      );
    };

};
